# Copiar a .streamlit/secrets.toml (no commitear secretos reales)
API_BASE_URL = "http://127.0.0.1:8000"

# En produccion conviene dejar la URL del API fija desde configuracion.
ALLOW_API_BASE_URL_OVERRIDE = false
